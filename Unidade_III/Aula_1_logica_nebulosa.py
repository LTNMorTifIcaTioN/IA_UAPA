# Lógica Nebulosa
"""
Lógica Nebulosa
> Permite que o problema seja definido de uma forma vaga, utilizando conceitos abstratos.
> A Lógicca Nebulosa prevê a existência de operadores lógicos equvalentes aos operadores NÃO, E e OU, da lógica
proposicional ou booleana:
    NÃO-FUZZY: NÃO-FUZZY(p) = 1-p
    E-FUZZY: E-FUZZY(x,y) = min(x,y)
    OU-FUZZY: OU-FUZZY(x,y) = max(x,y)
"""